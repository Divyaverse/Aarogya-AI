name: arogya_ai
description: "Arogya AI - Healthcare mobile application UI"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0 # For modern typography
  lucide_icons: ^0.257.0 # Clean, modern icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
