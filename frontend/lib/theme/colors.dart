import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette
  static const Color primary = Color(0xFF2F80ED); // Medical blue
  static const Color secondary = Color(0xFF56CCF2); // Soft blue
  
  // Status colors
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF2994A);
  static const Color danger = Color(0xFFEB5757);

  // Background and surface
  static const Color background = Color(0xFFF7FAFF);
  static const Color surface = Color(0xFFFFFFFF); // Card background
  
  // Text colors
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  
  // Additional helpful colors
  static const Color border = Color(0xFFE5E7EB);
  static const Color inputBackground = Color(0xFFF9FAFB);
}
